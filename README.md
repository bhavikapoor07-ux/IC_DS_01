# IC_DS_01
This project predicts whether a passenger survived the Titanic disaster using machine learning. It also predicts missing age values to improve model accuracy using Random Forest Regressor. The project focuses on handling the missing values of other columns too and leverages Random Forest Classifier to predict survival.
